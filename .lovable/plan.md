Generate a favicon from the "JC" navbar logo.

Steps:
1. Generate a square image (512×512) displaying "JC" in DM Serif Display, matching the site's editorial minimal aesthetic (dark text on light background, or inverted for dark mode compatibility).
2. Upload it as a project asset and copy to `public/favicon.png`.
3. Delete the old `public/favicon.ico`.
4. Add `<link rel="icon" href="/favicon.png" type="image/png" />` to `index.html`.

No other files will be changed.